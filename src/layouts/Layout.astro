---
import NavBar from "@components/NavBar.astro";
import Base from "./Base.astro";

interface Props {
  title: string;
  showNav: boolean;
}

const { title, showNav, ...rest } = Astro.props;
---

<Base title={title} {...rest}>
  {showNav && <NavBar />}
  <slot />
</Base>
