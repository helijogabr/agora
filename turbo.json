{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build:web": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".astro/**"]
    },
    "dev:web": {
      "dependsOn": ["push:dev"],
      "cache": false,
      "persistent": true
    },
    "preview:web": {
      "cache": false,
      "persistent": true
    },
    "push:dev": {
      "inputs": ["db/schema.ts", "drizzle.config.dev.ts"],
      "cache": false
    },
    "push:prod": {
      "inputs": ["db/schema.ts", "drizzle.config.ts"],
      "cache": false
    }
  }
}
